#!/usr/bin/env node

var colors = require('colors'),
    openBrowser = require('../lib/open-browser'),
    argv = require('optimist').argv,
    portfinder = require('portfinder'),
    fs = require('fs'),
    opener = require('opener');

if (argv.h || argv.help) {
  console.log([
    "usage: open-browser [path] [options]",
    "",
    "options:",
    "  -p                 Port to use [8080]",
    "  -a                 Address to use [0.0.0.0]",
    "  -d                 Show directory listings [true]",
    "  -i                 Display autoIndex [true]",
    "  -e --ext           Default file extension if none supplied [none]",
    "  -s --silent        Suppress log messages from output",
    "  --cors             Enable CORS via the 'Access-Control-Allow-Origin' header",
    "  -c                 Set cache time (in seconds). e.g. -c10 for 10 seconds.",
    "  -t                 Using template handler 'config.js'.",
    "                     To disable caching, use -c-1.",
    "  -h --help          Print this list and exit."
  ].join('\n'));
  process.exit();
}

var port = argv.p || parseInt(process.env.PORT, 10),
    host = argv.a || '0.0.0.0',
    log = (argv.s || argv.silent) ? (function () {}) : console.log,
    requestLogger;

if (!argv.s && !argv.silent) {
  requestLogger = function(req) {
    log('[%s] "%s %s" "%s"', (new Date).toUTCString(), req.method.cyan, req.url.cyan, req.headers['user-agent']);
  }
}

if (!port) {
  portfinder.basePort = 8080;
  portfinder.getPort(function (err, port) {
    if (err) throw err;
    listen(port);
  });
} else {
  listen(port);
}

function listen(port) {
  var options = {
    root: argv._[0],
    cache: argv.c,
    showDir: argv.d,
    autoIndex: argv.i,
    ext: argv.e || argv.ext,
    logFn: requestLogger
  };

  if (argv.t) {
    options.config = require('../lib/config');
  }

  fs.exists('./browser-config.js', function(exists) {
    if (exists) {
      options.config = require(process.cwd()+'/browser-config');
    }
  });

  if (argv.cors) {
    options.headers = { 'Access-Control-Allow-Origin': '*' };
  }

  var server = openBrowser.createServer(options);
  server.listen(port, host, function() {
    log('Starting up open-browser, serving '.yellow
      + server.root.cyan
      + ' on port: '.yellow
      + port.toString().cyan);
    log('Hit CTRL-C to stop the server');

    opener('http://127.0.0.1:' + port.toString());
  });
}

if (process.platform !== 'win32') {
  //
  // Signal handlers don't work on Windows.
  //
  process.on('SIGINT', function () {
    log('open-browser stopped.'.red);
    process.exit();
  });
}
